# Locale Template

Copy this folder and rename the new folder to your locale mod id.

Use `languages` to add a new language id, then provide a words patch. You can
also patch an existing language by omitting `languages` and using an existing
language id such as `en`, `sch`, or `modsch`.

Prefer Unicode escapes for non-ASCII text if your editor may change encoding.

